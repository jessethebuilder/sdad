<%= form_for(@sdad_obj) do |f| %>
  <% if @sdad_obj.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sdad_obj.errors.count, "error") %> prohibited this sdad_obj from being saved:</h2>

      <ul>
      <% @sdad_obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :published %><br>
    <%= f.check_box :published %>
  </div>
  <div class="field">
    <%= f.label :archived %><br>
    <%= f.check_box :archived %>
  </div>
  <div class="actions">
    <%= sdad_button_row(@sdad_obj, f) %>
  </div>
<% end %>
